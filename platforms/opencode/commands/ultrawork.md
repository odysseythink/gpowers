---
description: "Cross-platform verify-then-exit loop with independent Oracle verification — prevents premature "done" claims by requiring a fresh subagent to re-run verification and emit VERIFIED before exit"
---

<!-- SOURCE: $GPOWERS_HOME/core/skills/ultrawork/SKILL.md -->

This command invokes the gpowers skill **ultrawork** (core).

Refer to the source SKILL.md (above) for the full workflow. The platform's skill mechanism will load it on demand.
