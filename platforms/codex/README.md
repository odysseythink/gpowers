# gpowers — codex adapter

| | |
|---|---|
| Manifest file | `plugin.json` |
| Install link target | `~/.codex/plugins/gpowers` |
| Hooks | `true` |
| Namespace mode | `plugin-scoped` |

Generated by `gpowers-platforms gen codex`. To regenerate after upstream changes, run `gpowers upgrade` (Plan #9) followed by `gpowers-platforms gen codex`.

## Verifying

```bash
gpowers-platforms verify codex
```
