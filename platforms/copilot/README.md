# gpowers — copilot adapter

| | |
|---|---|
| Manifest file | `plugin.json` |
| Install link target | `~/.config/copilot-cli/plugins/gpowers` |
| Hooks | `false` |
| Namespace mode | `plugin-scoped` |

Generated by `gpowers-platforms gen copilot`. To regenerate after upstream changes, run `gpowers upgrade` (Plan #9) followed by `gpowers-platforms gen copilot`.

## Verifying

```bash
gpowers-platforms verify copilot
```
