# gpowers — cursor adapter

| | |
|---|---|
| Manifest file | `plugin.json` |
| Install link target | `~/.cursor/plugins/gpowers` |
| Hooks | `via-injection` |
| Namespace mode | `cursor-rules` |

Generated by `gpowers-platforms gen cursor`. To regenerate after upstream changes, run `gpowers upgrade` (Plan #9) followed by `gpowers-platforms gen cursor`.

## Verifying

```bash
gpowers-platforms verify cursor
```
