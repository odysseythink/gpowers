# gpowers — gemini adapter

| | |
|---|---|
| Manifest file | `extension.json` |
| Install link target | `~/.config/gemini/extensions/gpowers` |
| Hooks | `via-injection` |
| Namespace mode | `extension` |

Generated by `gpowers-platforms gen gemini`. To regenerate after upstream changes, run `gpowers upgrade` (Plan #9) followed by `gpowers-platforms gen gemini`.

## Verifying

```bash
gpowers-platforms verify gemini
```
