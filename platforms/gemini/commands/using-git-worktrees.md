---
description: "Use when starting feature work that needs isolation from current workspace or before executing implementation plans - ensures an isolated workspace exists via native tools or git worktree fallback"
---

<!-- SOURCE: $GPOWERS_HOME/core/skills/using-git-worktrees/SKILL.md -->

This command invokes the gpowers skill **using-git-worktrees** (core).

Refer to the source SKILL.md (above) for the full workflow. The platform's skill mechanism will load it on demand.
