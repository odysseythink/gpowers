---
name: setup-deploy
description: stub fixture for gstack setup-deploy
slash: /setup-deploy
---

# setup-deploy

This skill writes state to ~/.gstack/state and reads from ~/.gstack/config.
It invokes `gstack-setup-deploy` when needed. Cache lives under ~/.gstack/cache.
