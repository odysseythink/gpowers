---
name: ship
description: stub fixture for gstack ship
slash: /ship
---

# ship

This skill writes state to ~/.gstack/state and reads from ~/.gstack/config.
It invokes `gstack-ship` when needed. Cache lives under ~/.gstack/cache.
